# Restore-gap evidence: UUID 7fb868dc-3dd0-437a-9cc8-28f23a43fc70

Captured 2026-06-03 during the empirical pass that informs issues
**#32** (subagent / tool-results restoration) and **#33** (logger
session-states restoration).

## What this directory contains

| File | Contents |
|---|---|
| `preserve_manifest.json` | Preserve-tool manifest of all 189 files (with SHA256 hashes) that comprised the full on-disk footprint of session 7fb868dc before the test. Generated by `preserve MOVE --abs --hash SHA256 --recursive`. |
| `state-A-before-move.txt` | `find` output of every path under `~/.claude/` whose name contains the UUID, captured before any test action. 17 top-level entries (= 188 files when recursed). |
| `state-C-after-csb-restore.txt` | Same `find` output after `csb restore 7fb868dc-...` was run against the (cleared) tree. **1 entry**. |

## What the evidence shows

`csb restore` today materializes **1 out of 189 files** that constitute a session's full on-disk footprint:

- ✅ Restored: `projects/<slug>/<uuid>.jsonl` (the main transcript)
- ❌ Not restored: 188 other files across 7 path categories (subagents/, tool-results/, sesslogs/, session-states/, file-history/, plus runtime telemetry/debug/tasks/todos/session-env)

State A vs state C diff is the empirical proof that the gap covered by
#32 + #33 (and the open classification question for the remaining
categories) is real and large -- not speculation.

## Reuse

The `preserve_manifest.json` includes SHA256 hashes for every file and
its original absolute path. Useful as:

- Test fixture for whoever implements #32/#33 -- you have ground-truth
  before/after artifacts to compare against.
- Reference for "what does a full session footprint look like" across
  the seven `~/.claude/` subdirectories.
- Reproduction recipe -- the test was: `preserve MOVE` all matching
  paths out, run `csb restore`, observe gap, `preserve RESTORE` to
  put everything back. SHA256-verified roundtrip.

## What was NOT included in scope

By explicit exclusion to keep blast radius small:

- `~/.claude/projects/C--code-github-traffic-tracker/.session_cache.json` -- project-wide cache, not session-specific
- Sibling sessions `2d7a12ef-*` and `a9e6973e-*` -- different sessions
  in the same project
- `~/.claude/projects/C--code-github-traffic-tracker/memory/` -- project-level auto-memory

These remained untouched throughout the experiment.
