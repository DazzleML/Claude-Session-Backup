# v0.9.4 -- Feature -- view-index addressing + the liveness ancestry walk

Hand-runnable companion to `tests/test_view_index.py` (7) and the walk tests in `tests/test_liveness_pid.py` (TestHostWalk, 4). Covers #76 (grab roster rows by number), #78 (hook pid capture walked to the real claude host), the `csb status` polish, and the `set add` create-offer.

**Legend**: #76/#78 = the GitHub issues; "the walk" = the hook's process-ancestry climb past Claude Code's transient hook shell to the real claude CLI; "echo" = the `boot:15 -> NAME (uuid)` line printed per grabbed row.

## 1. View-index addressing (#76) -- LARGELY VERIFIED LIVE ALREADY

The maintainer's live tour (2026-08-07 evening) exercised: `set new NAME boot:1 boot:8 boot:15 boot:17 boot:18` with echoes, case-insensitive show, `set list` overlap "(4 open now)". Remaining spot-checks:

- [ ] `csb set rm <SET> <SET>:2` -- the self-referential remove, echo included.
- [ ] `csb set add <SET> last~2:1` -- history grab; the echo names what `show last~2` numbers 1.
- [ ] `csb set new X boot:999` -- aborts whole create (rc 2, "Nothing was changed"), no set exists after.
- [ ] A session named with a colon (if you have one) still resolves by name.

## 2. The ancestry walk (#78) -- THE POST-UPDATE PAYOFF

- [ ] `claude plugin update claude-session-backup@dazzle-claude-session-backup` (0.9.4).
- [ ] Open a fresh scratch `claude`; read its `csb-live/<uuid>.json`: the `pid` must be a claude CLI process (verify: `Get-Process -Id <pid>` -> claude), NOT a dead cmd/sh.
- [ ] `csb set current` -- the scratch session shows `[running]` from ORGANIC capture (no manual stamps).
- [ ] Cross-check with `tests/one-offs/probe_host_argv.ps1` run inside that session: the probe's claude pid == the entry's pid.
- [ ] Let any long-lived session compact: its entry's `pid` refreshes to a LIVE claude pid (`pid_at` updates; process exists).

## 3. `set add` create-offer

- [ ] TTY: `csb set add BRAND-NEW boot:1` -> `Create it with these members? [Y/n]` -> Y creates with echo + summary; re-run with n -> nothing created.
- [ ] Piped/scripted (`csb set add NOPE x | cat` or from a script): no prompt; the exact `csb set new ...` retry prints.

## 4. `csb status` polish -- APPROVED LIVE ALREADY

- [ ] One label column throughout; `Last scan` shows local time + UTC; colors degrade cleanly under `| cat` / rich-less.

## What the mocks don't cover

- The REAL hook-spawn chain shape (claude -> shell -> node -> python) across Claude Code versions -- the walk's whole reason; section 2 is the proof.
- Windows CIM vs POSIX `ps` table shapes feeding the walk on real machines.
- The TTY prompt feel of the create-offer.

## Planning lineage

- `dwp/2026-08-07__16-57-48__dev-workflow-process__v76-view-index-addressing.md` (V1-V8)
- Issues #76, #78 (ACs mirror); #79 filed for the did-you-mean follow-up
- The transient-shell evidence chain: two dead stamps on one entry, real host created the same second as the entry -- recorded in #78's body

## Cleanup

- [ ] Remove scratch sets (`csb set rm ...`) and close scratch sessions; `dz safedel` any scratch dirs.
